<div class="container">
    <div class="profile-main-body">


          <div class="row gutters-sm">
            <div class="col-md-4 mb-3">
              <div class="profile-card">
                <div class="profile-card-body">
                  <div class="d-flex flex-column align-items-center text-center">
                    <% if current_user.image.key.nil? %>
                      <%= image_tag "/assets/default_avatar.png", class: "rounded-circle", width: 150, alt: "Default Profile Picture" %>
                    <% else  %>
                      <%= cl_image_tag current_user.image.key, class: "rounded-circle", width: 150, height: 150, crop: :fill, alt: "Profile Picture"  %>
                    <% end %>
                    <div class="mt-3 name_container">
                      <h4><%= current_user.first_name %> <%= current_user.last_name %></h4>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-12 items-container">
                  <div class="profile-card h-100">
                    <div class="profile-card-body">
                      <h6 class="d-flex align-items-center mb-3"><i class="material-icons mr-2 bookmarks_titles">reservations</i>&nbsp Items</h6>
                          <% current_user.bookmarked_items.each do |item| %>
                              <% if  item.votes_for[0].vote_flag %>
                                <div class="card-chat">
                                  <%= cl_image_tag (item.image.key), class: "avatar-img mx-2"%>
                                  <%= link_to item.name, item_path(item) %>
                                  <%= link_to "Remove", downvote_item_path(item , type: "bookmark") ,data: {turbo_method: :put}, class: "unsave m-3" %>
                                </div>
                              <% else  %>
                                <%= nil %>
                              <% end %>
                        <% end %>
                    </div>
                  </div>
                </div>
            </div>
            <div class="col-md-8">
              <div class="profile-card mb-3">
                <div class="profile-card-body">
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">Email</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      <%= current_user.email %>
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">Gender</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      <%= current_user.gender %>
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">Address</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      <%= current_user.address %>
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-12 edit_btn_container">
                      <%= link_to 'Edit', edit_user_registration_path(current_user), class: "btn btn-info" %>
                    </div>
                  </div>
                </div>
              </div>

              <div class="row gutters-sm">
                <div class="col-sm-6 mb-3">
                  <div class="profile-card h-100">
                    <div class="profile-card-body">
                      <h6 class="d-flex align-items-center mb-3"><i class="material-icons mr-2 bookmarks_titles">bookmarks</i>&nbsp Events</h6>
                        <% current_user.bookmarked_events.each do |event| %>
                          <% if  event.votes_for[0].vote_flag %>
                            <div class="card-chat">
                              <%= cl_image_tag (event.image.key), class: "avatar-img mx-2"%>
                              <%= link_to event.name, event %>
                              <%= link_to "Remove", downvote_event_path(event , type: "bookmark") ,data: {turbo_method: :put}, class: "unsave m-3" %>
                            </div>
                          <% else  %>
                            <%= nil %>
                          <% end %>
                        <% end %>
                    </div>
                  </div>
                </div>
                <div class="col-sm-6 mb-3">
                  <div class="profile-card h-100">
                    <div class="profile-card-body">
                      <h6 class="d-flex align-items-center mb-3"><i class="material-icons mr-2 bookmarks_titles">bookmarks</i>&nbsp Babysitters</h6>
                        <% current_user.bookmarked_babysitters.each do |babysitter| %>
                          <% if  babysitter.votes_for[0].vote_flag %>
                            <div class="card-chat">
                              <%= cl_image_tag (babysitter.image.key), class: "avatar-img mx-2"%>
                              <%= link_to babysitter.first_name, babysitter %>
                              <%= link_to "Remove", downvote_babysitter_path(babysitter , type: "bookmark") ,data: {turbo_method: :put}, class: "unsave m-3" %></li>
                            </div>
                            <% else  %>
                              <%= nil %>
                            <% end %>
                        <% end %>
                    </div>
                  </div>
                </div>
              </div>



            </div>
          </div>

        </div>
    </div>
