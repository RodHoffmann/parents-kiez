<div class="container">
  <%= simple_form_for(item) do |f| %>
    <%= f.input :name %>
    <%= f.input :description%>
    <%= f.input :age, collection: Item::AGES, as: :radio_buttons %>
    <%= f.input :image, as: :file %>
    <%= f.submit "Save Changes!" %>
  <% end %>
</div>
